# Add Delete to Uploads (Sidebar Style)

## Steps:
- [x] 1. Add DELETE /api/uploads/:id in routes/uploads.js (user can delete own)
- [x] 2. Edit frontend/student-dashboard.html: Add red gradient delete button per card (🗑 top-right)
- [x] 3. Test delete → refresh, count updates (confirm → API call → reload list/stats)

